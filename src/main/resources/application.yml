spring:
  profiles:
    include: oauth

  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        # show_sql: true
        format_sql: true



logging.level:
  org:hibernate.SQL: debug

  org.springframework: info

